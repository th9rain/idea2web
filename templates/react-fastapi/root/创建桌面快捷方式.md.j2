# 📍 快捷方式创建指南

为了更方便地启动应用，您可以在桌面创建快捷方式。

---

## 🖱️ 创建启动快捷方式

### 方法 1：拖拽创建（最简单）

1. 打开文件资源管理器，进入项目目录：
   ```
   {{ project_path }}
   ```

2. 找到 `start.bat` 文件

3. **按住鼠标右键拖拽** `start.bat` 到桌面

4. 松开鼠标，选择"在此处创建快捷方式"

5. 重命名快捷方式为：`启动{{ app_name }}`

### 方法 2：右键菜单创建

1. 找到 `start.bat` 文件

2. **右键点击** → 选择"发送到" → "桌面快捷方式"

3. 在桌面找到快捷方式，重命名为：`启动{{ app_name }}`

---

## 🛑 创建停止快捷方式

用同样的方法为 `stop.bat` 创建快捷方式，命名为：`停止{{ app_name }}`

---

## 🎨 自定义快捷方式图标（可选）

如果您想让快捷方式更美观：

1. 右键点击快捷方式 → 选择"属性"

2. 点击"更改图标"按钮

3. 选择一个您喜欢的图标

4. 点击"确定"保存

---

## ✅ 完成后的效果

您的桌面上将有两个快捷方式：

- 📗 **启动{{ app_name }}** - 双击启动应用
- 🛑 **停止{{ app_name }}** - 双击停止应用

从此以后，启动应用就像打开其他软件一样简单！

---

**提示：** 第一次使用前，请确保已经按照"新手使用指南.md"完成了依赖安装。
