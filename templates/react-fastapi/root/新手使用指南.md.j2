# 🎯 新手使用指南

欢迎使用{{ app_name }}！这是一份完全为新手准备的使用指南。

---

## ⚡ 快速开始（一键启动）⭐

### Windows 用户

**只需一步：**
1. 双击根目录的 `start.bat` 文件

**自动完成的事情：**
- ✅ **智能健康检查** - 启动前全面检查环境
- ✅ **自动问题修复** - 自动修复常见问题（依赖缺失、端口占用等）
- ✅ 检查 Python 和 Node.js 环境
- ✅ 自动创建 Python 虚拟环境（如果不存在）
- ✅ **多镜像源安装** - 自动尝试清华/阿里/豆瓣镜像，确保安装成功
- ✅ **端口自动切换** - 端口被占用时自动切换到可用端口
- ✅ **数据库重试机制** - 初始化失败自动重试
- ✅ 启动后端服务（端口 {{ backend_port }}）
- ✅ 启动前端服务（端口 {{ frontend_port }}）
- ✅ 自动打开浏览器

**首次启动可能需要 2-5 分钟**（取决于网络速度）

---

## 📋 环境要求

在首次启动前，请确保已安装：

### 必需软件

1. **Python 3.8+**
   - 下载地址：https://www.python.org/downloads/
   - 安装时勾选"Add Python to PATH"

2. **Node.js 18+**
   - 下载地址：https://nodejs.org/
   - 选择 LTS 版本

### 验证安装

打开命令行，运行以下命令：

```bash
python --version    # 应显示 Python 3.8 或更高版本
npm --version       # 应显示 npm 版本号
```

如果显示"不是内部或外部命令"，说明未正确安装或未添加到环境变量。

---

### 启动应用

**方法 1：双击运行（推荐）**
- 在文件资源管理器中，找到 `start.bat` 文件
- **双击运行** `start.bat`
- 等待几秒钟，浏览器会自动打开

**方法 2：命令行运行**
```bash
cd {{ project_path }}
start.bat
```

启动成功后，会看到：
- ✅ 两个黑色窗口弹出（一个是后端，一个是前端）
- ✅ 浏览器自动打开到 http://localhost:{{ frontend_port }}
{% if has_auth -%}
- ✅ 显示登录页面
{% else -%}
- ✅ 显示应用主页面
{% endif %}

**⚠️ 重要提示：不要关闭那两个黑色窗口！关闭它们会停止服务。**

---

## 🚨 智能自愈系统

本项目内置了智能自愈系统，大多数问题会自动修复：

### 自动修复的问题

1. **依赖安装失败** → 自动尝试多个镜像源
2. **端口被占用** → 自动切换到可用端口并更新配置
3. **数据库初始化失败** → 自动重试 3 次
4. **虚拟环境缺失** → 自动创建
5. **依赖文件缺失** → 自动检测并提示

### 无法自动修复时

如果遇到无法自动修复的问题，系统会：

1. 生成详细的 `diagnostic_report.txt` 诊断报告
2. 报告包含：
   - 错误详情
   - 系统环境信息
   - 依赖版本信息
   - 建议的修复步骤
   - 复制给 AI 的格式化文本

**解决步骤：**
1. 打开项目根目录的 `diagnostic_report.txt`
2. 复制"复制以下内容给 AI 分析"部分
3. 粘贴给 AI 助手进行分析和修复

---

### 登录并使用

{% if has_auth -%}
**测试账号：**
- 用户名：`testuser`
- 密码：`password123`

登录后，你可以：
{% else -%}
打开应用后，你可以：
{% endif -%}
- 📖 查看预置的测试数据
- ✍️ 创建和管理数据
- 🔍 使用搜索和筛选功能
{% if has_stats -%}
- 📊 查看数据统计和可视化图表
{% endif %}

---

## 🛑 停止应用

**方法 1：双击运行（推荐）**
- 在文件资源管理器中，找到 `stop.bat` 文件
- **双击运行** `stop.bat`
- 等待几秒钟，所有服务自动关闭

**方法 2：手动关闭**
- 直接关闭那两个黑色窗口（前端和后端服务窗口）

**方法 3：命令行运行**
```bash
cd {{ project_path }}
stop.bat
```

---

## ❓ 常见问题

### Q1: Python 依赖安装失败怎么办？

**现象：** 启动时显示"Python 依赖安装失败"

**解决方案：**

1. **网络问题** - 脚本已使用清华镜像源，如果仍然失败，尝试：
   ```bash
   cd backend
   venv\Scripts\python.exe -m pip install -r requirements.txt
   ```

2. **权限问题** - 右键 `start.bat`，选择"以管理员身份运行"

3. **路径包含中文** - 将项目移动到纯英文路径（如 `C:\projects\{{ app_name }}`）

### Q2: 前端依赖安装失败怎么办？

**现象：** 启动时显示"前端依赖安装失败"

**解决方案：**

1. **网络问题** - 脚本已使用淘宝镜像源，如果仍然失败，尝试：
   ```bash
   cd frontend
   npm install --registry=https://registry.npmmirror.com
   ```

2. **npm 缓存问题** - 清理缓存后重试：
   ```bash
   npm cache clean --force
   npm install
   ```

3. **Node.js 版本过低** - 确保 Node.js 版本 >= 18

### Q3: 双击 start.bat 后没有反应？

**解决方案：**
1. 检查是否已经安装了 Python 和 Node.js
   - Python: 打开 cmd，输入 `python --version`
   - Node.js: 打开 cmd，输入 `node --version`
2. 确认 Python 和 Node.js 已添加到环境变量
3. 右键点击 `start.bat`，选择"以管理员身份运行"

### Q4: 提示"端口 {{ backend_port }} 或 {{ frontend_port }} 已被占用"？

**自动解决：**
- 系统会自动尝试切换到可用端口
- 配置文件会自动更新
- 无需手动干预

**手动解决：**
如果自动切换失败，可以：
1. 运行 `stop.bat` 停止旧服务
2. 如果 stop.bat 无效，手动查找并结束进程：
   ```bash
   netstat -ano | findstr :{{ backend_port }}
   taskkill /PID <进程ID> /F
   ```

### Q5: 浏览器打开了但页面是白屏？

**可能原因：**
- 前端依赖未完全安装
- Vite 构建失败

**解决方案：**
1. 关闭所有服务窗口
2. 删除 `frontend\node_modules` 目录
3. 重新双击 `start.bat`

### Q6: 页面显示但样式不正确？

**解决方案：**
1. 运行 `stop.bat` 停止服务
2. 重新运行 `start.bat` 启动服务
3. 浏览器打开后按 `Ctrl + F5` 强制刷新

### Q7: 生成了诊断报告怎么办？

**现象：** 启动时生成 `diagnostic_report.txt` 文件

**解决方案：**
这表示系统遇到了无法自动修复的问题。请：
1. 打开项目根目录的 `diagnostic_report.txt`
2. 查看报告中的"无法自动修复的错误"部分
3. 复制"复制以下内容给 AI 分析"部分
4. 粘贴给 AI 助手进行分析和修复

{% if has_auth -%}
### Q8: 忘记了测试账号密码？

**测试账号：**
- 用户名：`testuser`
- 密码：`password123`

### Q9: 想要创建自己的账号？

在登录页面点击"立即注册"，填写：
- 用户名（3-50 个字符）
- 邮箱（有效的邮箱格式）
- 密码（至少 6 位）
{% endif %}

---

## 🔧 高级操作

### 手动启动（高级用户）

如果自动启动失败，可以手动逐步启动：

**1. 安装后端依赖**

```bash
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

**2. 安装前端依赖**

```bash
cd frontend
npm install
```

**3. 启动服务**

打开两个命令行窗口：

**窗口 1 - 后端：**
```bash
cd backend
venv\Scripts\python.exe -m uvicorn app.main:app --reload --port {{ backend_port }}
```

**窗口 2 - 前端：**
```bash
cd frontend
npm run dev
```

访问：http://localhost:{{ frontend_port }}

---

### 查看 API 文档

浏览器打开：http://localhost:{{ backend_port }}/docs

可以看到所有 API 接口的详细说明和在线测试功能。

### 查看数据库内容

使用 SQLite 工具打开 `backend/app.db` 文件即可查看所有数据。

### 重置数据库

如果想清空所有数据并重新生成测试数据：

```bash
cd backend
del app.db
venv\Scripts\python.exe seed.py
```

---

## 📞 需要帮助？

如果遇到问题：
1. 查看上方的"常见问题"部分
2. 检查两个服务窗口的错误信息
3. 查看 README.md 获取更多技术细节

---

## 🎉 开始使用吧！

现在你已经掌握了所有基础操作，可以开始使用{{ app_name }}了！

**祝你使用愉快！✨**
